version: 2.1

jobs:
  run-bot:
    docker:
      - image: cimg/php:8.2
    parameters:
      account:
        type: string
    steps:
      - checkout
      - run:
          name: Set executable permission
          command: chmod +x captcoin/captcoin.php
      - run:
          name: Run Captcoin.php
          command: |
            USERNAME=gtg<< parameters.account >> PASSWORD=memeX3213! php captcoin/captcoin.php

workflows:
  matrix-workflow:
    jobs:
      - run-bot:
          matrix:
            parameters:
              account: ["1", "2", "3"]
